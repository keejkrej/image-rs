# Fixtures

This directory contains reusable test and demo assets for `image-rs`.

## Recipes

- `recipes/normalize-threshold.json`: normalize then fixed threshold.
- `recipes/analysis-basic.yaml`: blur, Otsu threshold, morphology, measurements.

These files are suitable for `ijrs run --recipe <path>`.
